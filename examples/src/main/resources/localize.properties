# This is the default ICU4J formatter, so specifying it here is optional, as well as this file.
localize.icu4j.formatter = com.ibm.icu.message2.MessageFormatter

# To use the legacy ICU4J MessageFormat syntax instead, uncomment the line below:
# Note: The new and legacy message format syntax are incompatible.
#
# localize.icu4j.formatter = com.ibm.icu.text.MessageFormat

# The ICU4J formatter can also be set as a JVM system property:
#   -Dlocalize.icu4j.formatter=com.ibm.icu.text.MessageFormat
